unit EmailFeedback;

interface

uses
  Winapi.Windows, Winapi.Messages, System.SysUtils, System.Variants, System.Classes, Vcl.Graphics,
  Vcl.Controls, Vcl.Forms, Vcl.Dialogs, Vcl.StdCtrls,
  ShellAPI;

type
  TEmailForm = class(TForm)
    Memo1: TMemo;
    EmailButton: TButton;
    Label1: TLabel;
    Label2: TLabel;
    Label3: TLabel;
    procedure EmailButtonClick(Sender: TObject);
    procedure Memo1Change(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  EmailForm: TEmailForm;

implementation

{$R *.dfm}

procedure TEmailForm.EmailButtonClick(Sender: TObject);
var email: string;
    RetVal: integer;
begin
    email := 'mailto:richersimulations@gmail.com'
    + '?subject=TBPB 2020 '+EmailForm.Caption
    + '&body='Memo1.Text;
  RetVal := ShellExecute(Handle, 'open', PChar(email), nil, nil, SW_SHOWNORMAL);
  if RetVal <= 32 then
    MessageDlg('Cannot find program to send e-mail.', mtWarning, [mbOK], 0);

  EmailForm.Close;
end;

procedure TEmailForm.Memo1Change(Sender: TObject);
begin
  Label3.Caption := IntToStr(255-length(Memo1.Text))+' characters left';
end;

end.
